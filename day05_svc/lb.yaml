apiVersion: v1
kind: Service
metadata:
  name: svc-lb
  labels:
    env: demo
spec:
  type: LoadBalancer
  selector:
    app: nginx-depl
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80