apiVersion: v1
kind: Service
metadata:
  name: svc-np
  labels:
    env: demo
spec:
  type: NodePort
  selector:
    app: nginx-depl
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
      nodePort: 30031
