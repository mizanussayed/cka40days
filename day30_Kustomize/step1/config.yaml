apiVersion: v1
kind: ConfigMap
metadata:
  name: myapp-config
data:
  config.properties: |
    # This is a sample configuration file for myapp
    setting1=value1
    setting2=value2
    setting3=value3