apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: prod

resources:
  - ../base
  - sealedsecret.yaml

patches:
  - target:
      kind: Deployment
      name: dotnet-api
    patch: |-
      - op: replace
        path: /spec/replicas
        value: 3
