apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.database.name }}-service
spec:
  clusterIP: None
  selector:
    app: {{ .Values.database.name }}
  ports:
    - port: 5432
      targetPort: 5432
